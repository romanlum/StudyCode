#include <stdio.h>

void swap(int *x, int *y) {
  int tmp=*x;
  *x=*y;
  *y=tmp;
}

int main() {
  int x,y;
  x = 10;
  y = 20;

  printf("x= %d, y= %d", x, y);
  swap(&x, &y);
  printf("x= %d, y= %d", x, y);
  return 0;  
}
